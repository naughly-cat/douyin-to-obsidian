# 安全策略

## 支持范围

当前仅维护最新的 `0.1.x` 版本。项目仍处于早期阶段，解析规则可能随平台页面变化而失效。

## 报告漏洞

请不要通过公开 Issue 报告尚未修复的漏洞。仓库发布后，请在 GitHub 仓库的 **Security** 页面使用 **Report a vulnerability** 私下提交，并提供：

- 受影响版本和操作系统；
- 可复现的最小步骤；
- 预期影响；
- 已做脱敏的日志或样例数据。

尤其欢迎报告 TLS 降级、路径覆盖、Markdown/YAML 注入、模型供应链、任意文件写入，以及恶意媒体文件处理相关问题。

维护者会尽量在 7 天内确认收到报告。修复公开前，请避免披露可直接利用的细节。

## 安全边界

- HTTPS 默认严格校验；`--insecure` 是有中间人攻击风险的显式临时开关。
- 自动下载的 Whisper 模型必须命中内置 SHA256；未知模型会被拒绝。
- 本项目会把远程媒体交给本机 `ffmpeg` 处理，请及时更新系统中的 `ffmpeg`。
- 不要处理来源不明的私密链接、Cookie、访问令牌或包含敏感个人信息的数据集。
